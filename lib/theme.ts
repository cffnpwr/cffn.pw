import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: 'Ubuntu, \'M PLUS Rounded 1c\', sans-serif',
    body: 'Ubuntu, \'M PLUS Rounded 1c\', sans-serif',
  },
});

export default theme;